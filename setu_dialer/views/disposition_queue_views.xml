<odoo>
    <!-- Tree View -->
    <record id="view_disposition_queue_tree" model="ir.ui.view">
        <field name="name">disposition.queue.tree</field>
        <field name="model">disposition.queue</field>
        <field name="arch" type="xml">
            <tree>
                <field name="lead_id"/>
                <field name="campaign_id"/>
                <field name="lead_list_id"/>
                <field name="disposition_id"/>
                <field name="call_by"/>
                <field name="disposition_time"/>
                <field name="disposition_from"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_disposition_queue_form" model="ir.ui.view">
        <field name="name">disposition.queue.form</field>
        <field name="model">disposition.queue</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="lead_id"/>
                        <field name="campaign_id"/>
                        <field name="lead_list_id"/>
                        <field name="disposition_id"/>
                        <field name="call_by"/>
                        <field name="disposition_time"/>
                        <field name="disposition_from"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_disposition_queue" model="ir.actions.act_window">
        <field name="name">Disposition Queue</field>
        <field name="res_model">disposition.queue</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>
