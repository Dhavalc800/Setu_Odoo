<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2019 Akretion
    License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl). -->
<odoo>
    <record id="sale_order_view_form" model="ir.ui.view">
      <field name="model">sale.order</field>
      <field name="inherit_id" ref="sale.view_order_form" />
      <field name="priority" eval="15"/>
      <field name="arch" type="xml">
          <xpath expr="//header" position="after">
              <header name="substate">
                  <field
                        name="substate_id"
                        widget="statusbar"
                        options="{'clickable': '1',}"
                    />
                    <!-- <field name="is_payment_verification" invisible="1"/> -->
                    <!-- <field name="is_payment_received" invisible="1"/> -->
                    <!-- <button name="request_payment_verification" type="object" string="Payment Under Verification"
                            data-hotkey="r" class="oe_highlight"
                            attrs="{'invisible': ['|',('state', 'in', [ 'draft', 'sale']), ('is_payment_verification', '=', False)]}"
                            groups="account.group_account_invoice"
                    /> -->
                    <!-- Commented button as client asked to remove -->
<!--                    <button name="payment_received" type="object" string="Payment Received" data-hotkey="e" class="oe_highlight" attrs="{'invisible': ['|',('state', '!=' , 'sent' ), ('is_payment_received', '=', True)]}"/>-->
              </header>
          </xpath>
      </field>
    </record>

    <record id="view_order_tree" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="substate_id" />
            </field>
        </field>
    </record>

    <record id="view_quotation_tree" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree" />
        <field name="arch" type="xml">
            <field name="state" position="after">
                <field name="substate_id" />
            </field>
        </field>
    </record>

    <record id="view_sales_order_filter" model="ir.ui.view">
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_sales_order_filter" />
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="substate_id" />
            </field>
            <filter name="order_month" position="after">
                <filter
                    name="substate_id"
                    string="Substate"
                    domain="[]"
                    context="{'group_by':'substate_id'}"
                />
            </filter>
        </field>
    </record>

    <!-- Inherited action to remove it from booking action -->
    <record id="sale.action_sale_order_generate_link" model="ir.actions.act_window">
        <field name="name">Generate a Payment Link</field>
        <field name="res_model">payment.link.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="payment.payment_link_wizard_view_form"/>
        <field name="target">new</field>
        <field name="binding_model_id" ref="sale.model_sale_order"/>
        <!-- <field name="binding_model_id" ref="model_sale_order"/> -->
        <field name="binding_view_types">form</field>
    </record>

</odoo>
